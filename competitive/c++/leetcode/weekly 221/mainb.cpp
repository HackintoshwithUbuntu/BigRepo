#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int eatenApples(vector<int>& apples, vector<int>& days) {
        bool unfin = true;

        vector<int> aps;
        for(int i = 0; i < apples.size(); i++){
            break;
        }


        while(unfin){
            break;
        }
    }
};