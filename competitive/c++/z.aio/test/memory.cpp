#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("ladyin.txt", "r", stdin);
    freopen("ladyout.txt", "w", stdout);

    while (true)
      long double *a = new long double;  // allocating 
}